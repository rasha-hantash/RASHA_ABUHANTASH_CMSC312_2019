package OS;
public class Memory {

    public static int memorySize = 4096; 

    //subtract whenever adding a process onto memory 
    //if changing a process from waiting to ready becuase of CALC
    // and there is no memory just add it onto the end of the waiting queue

}